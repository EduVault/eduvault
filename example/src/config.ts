// import dotenv from 'dotenv';
// dotenv.config({ path: '../../.env' });

export const APP_SECRET = process.env.VUE_APP_APP_SECRET;
console.log(process.env);
// import { config } from '@eduvault/shared';

export const STORAGE_KEY = 'sourcelink';
